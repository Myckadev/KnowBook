{% extends 'basePanel.html.twig' %}

{% block title %}
    {{ parent() }} Vos paramètres
{% endblock %}

{% block body %}

    <div style="width: 80%; margin: 0 auto">
        <div style="background: #fff;border-radius: 10px; overflow: hidden; margin-top: 20px">
            <div style="width: 80%; margin: 0 auto;">
                <h1>Vos paramètres</h1>


                <div class="wrap-input100 validate-input" style="margin-bottom: 15px;">
                    <div style="; display: flex; justify-content: space-between; font-size: 15px; line-height: 1.5;
    height: 50px; border-radius: 25px; padding: 15px 30px 0 68px">
                        <span><a href="{{ path('app_forgot_password_request') }}"><button>Changez de mot de passe ?</button></a></span>
{#                        <span><a href="{{ path('AppDeluser', {'id': app.user.id}) }}"><button>Supprimer votre compte ?</button></a></span>#}
                    </div>
                </div>

            </div>
        </div>
    </div>

{% endblock %}


