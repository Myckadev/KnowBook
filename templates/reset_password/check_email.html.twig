{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Email changement mot de passe envoyé{% endblock %}

{% block body %}


    <div class="container-login100">
        <div class="wrap-login100">
            <div class="login100-form validate-form" method="post"><span class="login100-form-title">Choisir un nouveau mot de passe</span>
                <p>
                    Si un compte existe à l'email renseigné, alors vous recevrez un lien dans votre boite mail.
                    Le lien expirera d'ici {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p>Si vous n'avez pas d'email, verifiez vos spam ou  <a href="{{ path('app_forgot_password_request') }}" style="color: cornflowerblue">réessayez</a>.</p>
                <div class="container-login100-form-btn">
                    <a href="{{ path('AppHome') }}">
                        <button class="login100-form-btn" type="submit">
                            Accueil
                        </button>
                    </a>
                </div>
            </div>
        </div>
    </div>

{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('logasset/login.css') }}">
    <link rel="stylesheet" type="text/css" href="fonts/font-awesome-4.7.0/css/font-awesome.min.css">
{% endblock %}